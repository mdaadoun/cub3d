* payer must move relative to its direction angle (fix A S D W move)

* init flr and cel rectangle from width and height
* draw window, draw two rectangle, top for celling, bottom for floor

* draw map of 64x64 squares
* draw 4 walls, 4 differents colors
* cone of vision, detect wall

* set player position x, y
* move player in x, y position inside a square offset of 64x64 pixels

* draw first ray cast, from player to first wall side
* rotate vision cone of player
* connect each input event to movement or player or vision cone (A S D W LEFT RIGHT)

END
* add headers