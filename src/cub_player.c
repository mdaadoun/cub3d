#include "../inc/cub.h"

void	cub_rotate_player(t_cub *cub, t_u16 key)
{
	ft_printf("rotate %d\n", key);
	(void) cub;
}

void	cub_move_player(t_cub *cub, t_u16 key)
{
	ft_printf("move %d\n", key);
	(void) cub;
}